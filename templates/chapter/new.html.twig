{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nuevo Capítulo</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Guardar</button>
    {{ form_end(form) }}
{% endblock %}