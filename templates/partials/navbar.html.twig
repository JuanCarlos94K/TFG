<nav class="bg-white border-b border-gray-200 px-4 py-3">
  <div class="container mx-auto flex items-center justify-between">
    <a href="{{ path('homepage') }}" class="text-xl font-bold text-gray-800">Mi Blog</a>
    <div class="space-x-4">
      <a href="{{ path('homepage') }}" class="text-gray-600 hover:text-blue-600">Inicio</a>
    </div>
  </div>
</nav>