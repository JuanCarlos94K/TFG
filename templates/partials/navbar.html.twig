<nav class="bg-white shadow-card rounded mb-8 p-4 flex justify-between items-center">
    <a href="{{ path('homepage') }}" class="text-accent font-bold text-lg">Mi Blog</a>
    <ul class="flex space-x-6 text-secondary">
        <li><a href="{{ path('homepage') }}" class="hover:border-b hover:border-accent transition">Inicio</a></li>
        <li><a href="{{ path('chapters_index') }}" class="hover:border-b hover:border-accent transition">Capítulos</a></li>
        <!-- otros enlaces -->
    </ul>
</nav>